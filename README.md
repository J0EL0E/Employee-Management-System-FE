# React + Vite
# Frontend Setup (React)

## 3.1 Installation and Environment Setup

### Clone the Repository

Clone the frontend repository:

```bash
git clone (https://github.com/J0EL0E/Employee-Management-System-FE)
cd Employee-Management-System-FE
Install Dependencies
Install all required Node.js packages:

bash
Copy code
npm install
Configure Environment Variables
Create a .env file in the root of the frontend project to point the frontend to the running backend:

env
Copy code
VITE_API_HOST=http://localhost:4000
Start the Development Server
Run the frontend development server:

bash
Copy code
npm run dev
3.2 Frontend Technology Stack
Component	Technology	Role
UI	React	Core library for rendering components
Styling	Tailwind CSS	Utility-first CSS framework for styling
Routing	React Router DOM	Manages client-side navigation
Data Fetching	Axios	Promise-based HTTP client for API requests
